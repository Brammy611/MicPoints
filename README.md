MicPoints

MicPoints is a lightweight Python-based application that includes a simple server component.
This project appears to provide point-tracking or microservice-like functionality (update this description if needed).

📌 Features
- Pure Python project
- Contains a main application script (main.py)
- Server component separated into server.py
- Easy to extend and customize
- Dependency installation through requirements.txt

📁 Project Structure
MicPoints/
│
├── main.py
├── server.py
└── requirements.txt

🚀 Installation
1. Clone the repository
   git clone https://github.com/Brammy611/MicPoints.git
   cd MicPoints
2. Install dependencies
   pip install -r requirements.txt
3. Run main on Thonny IDE/Arduino IDE
